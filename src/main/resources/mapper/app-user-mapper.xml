<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "classpath:xsd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.app.user.mapper.AppUserMapper">

    <!-- 로그인 -->
    <select id="getAppUserById" parameterType="string" resultType="AppUser">
    </select>

    <!-- 회원가입 -->
    <insert id="insertAppUser" parameterType="AppUser">
    </insert>

</mapper>